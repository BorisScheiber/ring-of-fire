export interface SingleGameData {
    currentPlayer: number;
    playedCards: string[];
    players: string[];
    stack: string[];
    pickCardAnimation: boolean;  // Neu
    currentCard: string;        // Neu
}